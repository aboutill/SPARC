TODO:

cardiac-116 deployment:
- copy scripts into /home/ab23/Software/SPARC/scripts + makefile + run make install PREFIX=/usr/local/bin
- For each user (tw, sdu, lf, dl) copy cfg/pipeline.yaml + scripts/runPipeline.bash into /home/{user}/fcmr_4D_recon + instructions_abridged.txt
  Each folder should have the subfolders: ./data ./recon ./cfg/pipeline.yaml ./scripts/runPipeline.bash instructions_abridged.txt
  Remove everything else (old scripts)
- remove fcmr_reconstruction_app docker

SVR-lite + dockerfile:
- build svr-lite from source with public github link
- review dockerfile base utilities
- most likely update to latest release of ITKSNAP
- download from huggingface before copy ./src ? (to prevent download at each build)
- image tag 'v1.0.0' 'latest' should be an option to all bash scripts
